# ── Scratchpad / working notes (keep out of repo) ──
*.txt
!requirements.txt
!package-lock.txt

# ── OS cruft ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor/IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Node / build artifacts (in case we ever add them) ──
node_modules/
dist/
build/
.env
.env.local
.dev.vars

# ── Cloudflare / Wrangler local state ──
.wrangler/
.mf/

# ── Local credentials ──
*.pem
*.key
secrets.json
